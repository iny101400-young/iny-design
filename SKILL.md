---
name: iny-design
description: 04단계. 03 이 만든 구조를 받아 사이트 화면을 그린다. 화면 종류를 정하고 메뉴를 짜고 색과 글꼴을 확정하고, 화면마다 요소를 다섯 층으로 쪼개 개발을 모르는 사람도 읽는 화면설계서 한 장을 낸다. 글 한 편마다 그리지 않고 같은 모양으로 나오는 것은 한 장으로 묶는다. 03 이 준 것 중 어느 화면에도 안 들어간 것이 없는지 검사한다. 여기서 나온 화면설계서가 05 기술문서와 06 사이트 짓기의 뼈대가 된다. 트리거는 "화면 그리기", "화면설계", "화면설계서", "사이트 화면 그려줘", "메뉴 짜기", "디자인 정하기", "iny-design", "04 시작".
---

# 04 DESIGN · 화면 그리기

`v1.0`

백지에서 쓴다. 남의 저장소에서 파일을 가져오지 않는다.
설계를 참고한 곳은 [`references/디자인규칙.md`](references/디자인규칙.md) 맨 끝에 적어 뒀다.

## ⚑ 실행 순서

건너뛰지 않는다.

| STEP | 무엇 | 멈추나 |
|---|---|---|
| **1** | **안내문을 낸다** (아래 「첫 메시지」 그대로) | ✋ 답할 때까지 |
| **2** | 03·02 산출물을 읽는다. 없으면 03 부터 하시라고 안내하고 멈춘다 | |
| **3** | 클릭 ① 디자인을 어디서 가져올까 | ✋ |
| **4** | 값을 뽑아 표로 되보여준다 · 클릭 ② | ✋ |
| **5** | **화면 종류와 메뉴 후보를 채워 낸다** · 클릭 ③ | ✋ |
| **6** | **「만든 것」 목록을 캐서 채워 놓는다** · 클릭 ④ | ✋ |
| **7** | 클릭 ⑤ 연락 칸을 둘지 | ✋ |
| **8** | **화면 그리기.** 화면에는 한 줄만 | |
| **9** | 읽을 사람으로 검토하고 고친다 · 자리 검사 | |
| **10** | **문서 셋을 쓰고 `plan.md` 와 05 안내를 한 덩어리로 낸다** | ✋ 마지막 |

**STEP 1 을 건너뛰지 않는다.** "화면 그리기" 라고만 해도 안내문부터 낸다.
설정이나 03 산출물부터 읽고 그 상태를 보고하지 않는다.

**순서가 보드와 다르다.** 보드는 「화면 구조 → 디자인 컨셉」인데 **디자인이 먼저다.**
디자인이 정해져야 미리보기를 진짜 색으로 그린다. 그래야 **화면을 한 번만 그린다.**
디자인을 나중에 정하면 그려둔 화면을 통째로 다시 그려야 한다.

## ⚑ 꼬리를 빠뜨리지 않는다

**02 1회차에 마지막 두 STEP 이 통째로 날아갔다.** 확인받는 자리에서 대화가 끝났고
그 뒤에 있던 것이 전부 밀렸다.

> **확인받는 자리 뒤에 뭘 두지 않는다. 거기서 세션이 끝난다.**

| | |
|---|---|
| 1 | **STEP 10 이 마지막이다.** 문서 쓰기 · `plan.md` 보여주기 · 05 안내를 **한 덩어리로** 낸다 |
| 2 | 그 덩어리의 **맨 끝**이 "고칠 데 말씀해 주세요" 다. 그 뒤에 아무것도 없다 |
| 3 | **`plan.md` 「다음에 할 일」에 이 스킬이 할 일을 적지 않는다.** 거기 적힌 건 사람이 나중에 할 일이다 |
| 4 | 산출물에 **「나중에」 · 「필요하면」 · 「사용자가 직접」** 을 쓰지 않는다 |

## ⚑ 04 는 출력이 커져서 걸린다

**앞 세 단계와 걸리는 자리가 반대다.**

```
01 · 02 · 03    입력이 커서 걸렸다     재료가 수천 개
04              출력이 커서 걸린다     화면설계서가 수십만 자
```

03 이 넘긴 것으로 주소를 세면 수십 개가 나온다. 큰 갈래마다 하나, 작은 갈래마다 하나,
글 한 편마다 하나. **주소마다 화면을 한 장씩 그리면 한 파일에 다 못 담고, 담아도 못 읽는다.**

읽을 수 있는 크기가 만들 수 있는 크기보다 먼저 걸린다.

| | |
|---|---|
| 만들 수 있는 크기 | 한 파일에 십수만 자 |
| **읽을 수 있는 크기** | **그보다 한참 아래다.** 화면에 닿기 전에 지나야 하는 글이 길면 아무도 안 읽는다 |

**그래서 처음부터 파일을 둘로 나눈다.** `화면설계서.html` 에는 화면만 넣고
근거 · 디자인 값 · 안 하는 것은 `근거.html` 로 뺀다. **다 쓰고 나서 쪼개지 않는다.**

## ⚑ 지킬 수 없는 약속을 하지 않는다

**「페이지마다 한 장」이라고 말하지 않는다.**

글은 07 이 한 편 쓸 때마다 는다. 페이지마다 한 장이면 **글을 쓸 때마다 화면설계서를 고쳐야 한다.**
04 는 한 번만 지나는 앞단인데 뒷단이 돌 때마다 04 산출물이 낡는다.

> **화면 종류마다 한 장.** 같은 틀로 나오는 것은 한 장으로 묶고 몇 개가 그 틀을 쓰는지 적는다.

**글은 계속 늘고 화면 종류는 안 는다.**

**장수를 미리 약속하지 않는다.** 조건 넷을 걸고 다 그린 뒤에 몇 장인지 말한다.

| | 조건 |
|---|---|
| 1 | **03 이 준 것이 전부 어느 화면엔가 자리를 갖는다.** 큰 갈래 · 작은 갈래 · 글 · 비어 있는 것 |
| 2 | **어느 화면에도 안 들어간 03 산출물이 0개다.** STEP 9 에서 기계로 잰다 |
| 3 | **같은 틀로 나오는 것은 한 장으로 묶는다.** 묶은 것은 몇 개를 덮는지 적는다 |
| 4 | **글이 0편일 때의 화면을 같이 그린다.** 지금 있는 건 글이 아니라 뼈대다 |

**조건 3 이 없으면 읽는 사람이 「내 글 수십 편은 어디 갔지」 하고 못 찾는다.**
묶은 화면마다 한 줄을 붙인다.

```
글 한 편          1장   ← 글 {N}편이 이 틀을 씁니다
작은 갈래 목록     1장   ← 갈래 {N}개가 이 틀을 씁니다
```

## ⚑ 예시를 지어낸다

**04 가 이 체인에서 제일 위험한 자리다.**

앞사람이 만든 화면설계서가 잘 나와 있으면 그걸 본보기로 삼고 싶어진다.
**그 안에 든 건 전부 한 사람의 값이다.** 색도 그 사람이 고른 것이고 숫자도 그 사람 자료다.

| 박히면 안 되는 것 | |
|---|---|
| 색 코드 | 누가 고른 보라색이든 초록색이든 |
| 첫 화면 문장 | 그 사람이 쓴 카피 |
| 근거 숫자 | 몇 분에서 몇 분 · 몇 건에서 몇 건 |
| 메뉴 이름 | 그 사람이 고른 탭 |
| 태그 · 갈래 이름 | 그 사람 주제에서 나온 것 |
| 레퍼런스 사이트 이름 | 그 사람이 좋아한 곳 |

> **규칙. 이 문서에 쓰는 예시는 전부 지어낸 것이다.
> 그리고 「지어낸 예시입니다」라고 적는다.**

**이 규칙은 이 파일을 고칠 때마다 다시 걸린다.** 규칙을 세운 사람이 새 절을 쓰면서
실제 산출물의 문장을 그대로 옮긴 적이 앞 단계에서 여러 번 있었다.
**고칠 때마다 `grep` 을 돌리는 것 말고는 되는 방법이 없다.**

## ⚑ 레퍼런스는 남의 저작물이다

**04 는 「좋아하는 사이트 링크나 이미지」를 받는다.** 앞 세 단계에 없던 자리다.

| | |
|---|---|
| 받은 그림을 산출물에 넣으면 | **남의 저작물이 우리 문서로 들어온다.** 그 문서가 06 을 거쳐 사이트가 된다 |
| **그래서** | **값만 가져온다.** 색 · 굵기 · 간격 · 움직이는 거리 |

> **레퍼런스에서 값만 가져온다. 그림을 산출물에 넣지 않는다. 어디서 가져왔는지는 적는다.**

받은 이미지 파일도 마찬가지다. **열어서 값을 읽고 문서에는 값만 적는다.**
파일을 복사해 두지 않고 `근거.html` 에 「어느 화면에서 무슨 값을 봤다」로 남긴다.

## ⚑ 화면설계서에 사진을 넣지 않는다

미리보기를 스크린샷으로 채우면 파일이 폭발한다. 이미지 수십 장을 데이터로 박으면 수십 MB 다.
01 이 화면 출력 크기에서 막힌 것과 같은 계열이다.

> **미리보기는 전부 CSS 로 그린다.** 진짜 색과 진짜 문장으로 그리되 사진은 안 넣는다.

## ⚑ 첫 화면 뜨는 시간을 나쁘게 하는 것

무겁게 움직이는 것을 고를 때는 **그 값을 같이 말한다.**

첫 화면 뜨는 시간이 나빠지면 나중에 유입이 안 늘었을 때
**「구조가 안 먹힌 건가, 느려서인가」를 못 가른다.** 08 이 재는 것을 04 가 망칠 수 있다.

**금지하지는 않는다.** 고르는 사람에게 그 값을 말하고 고르게 한다.

## ⚑ 첫 화면에 내면 안 되는 말

**"03 산출물 {N}자를 읽었고 주소가 {N}개 나옵니다" 같은 말을 첫 화면에 내지 않는다.**
우리한테 필요한 말이지 쓰는 사람한테는 겁만 준다.

설정 읽기, 03 산출물 읽기, 「만든 것」 캐기는 **조용히** 한다.
`허브` · `클러스터` · `다섯 층` 같은 말은 설명 없이 쓰지 않는다.

## ⚑ 출력이 터지지 않게 한다

01 이 화면 출력에서 막혔고 03 은 위키 목차가 그 자리였다. **04 는 화면 목록이 그 자리다.**

| | |
|---|---|
| 화면 목록 | **화면 종류 단위로** 낸다. 주소를 다 나열하지 않는다 |
| 글 목록 | **개수만.** 글 제목을 화면에 쏟지 않는다. `구조.md` 에 이미 있다 |
| 요소 목록 | 화면에 안 낸다. **화면설계서 파일 안에 있다** |
| 검토 결과 | **고친 것의 개수와 종류**만. 목록은 `근거.html` 에 있다 |

## ⚑ 화면에 낼 때 쓰는 말

**04 는 풀어 쓸 말이 제일 많은 단계다.** 받아 쓰는 사람은 이 말을 모른다.
모르면 물어보지도 못하고 그냥 안 쓴다.

이 문서 안에서는 그대로 쓴다. 파일 안도 그대로다. **화면에만 바꿔 쓴다.**

| 우리 말 | 화면에 낼 말 |
|---|---|
| 와이어프레임 · 목업 | **화면 미리보기** |
| 레이어 · 층 | **아래 다섯 가지로 풀어서** |
| 반응형 | **폰에서 볼 때** |
| 컴포넌트 | **되풀이 쓰는 조각** |
| 타이포그래피 | **글자 크기와 굵기** |
| 그리드 | **칸 나누기** |
| 히어로 | **첫 화면** |
| CTA | **누르게 하는 버튼** |
| 스키마 | **기계가 읽는 표시** |
| 인터랙션 · 모션 | **움직임** |
| 라우팅 · 슬러그 | **주소** |
| 허브 · 클러스터 | **큰 갈래 · 작은 갈래** (03 과 같게) |
| 디자인 토큰 | **정해 놓고 되풀이 쓰는 값** |

### 다섯 층은 이렇게 푼다

`UI` `Logic` `Data` `API` `Deploy` 는 **파일 안에 영문 그대로 둔다.**
05 와 06 이 그 이름으로 읽는다. **바꾸는 건 화면에 내는 말과 문서의 설명 상자뿐이다.**

| 층 | 화면에 낼 말 | 안 될 때 하는 말 |
|---|---|---|
| UI | 보이는 것 | "버튼인지 모르겠다" |
| Logic | 누르면 일어나는 일 | "눌러도 반응이 없다" |
| Data | 글이 어디서 오나 | "제목이 안 나온다" |
| API | 바깥 서비스와 잇는 통로 | "메일이 안 간다" |
| Deploy | 인터넷에 올리는 것 | "내 컴퓨터에선 되는데" |

**「안 될 때 하는 말」 칸이 이 표의 핵심이다.**
개발을 모르는 사람이 다섯을 구분하는 유일한 방법이 그거다.

### 처음 한 번은 풀어서 같이 적는다

그 말이 처음 나오는 자리에서만 괄호로 원래 말을 붙인다. **두 번째부터는 안 붙인다.**

## ⚑ 오탈자

보내기 전에 한 번 읽는다. 한 글자 틀리면 "대충 만들었네"로 읽히고 그 뒤 신뢰가 통째로 떨어진다.
**아래 「고정 문구」에 적힌 문장은 그대로 복사해서 쓴다.** 즉흥으로 다시 쓰지 않는다.

---

## 이 스킬이 진짜 하는 일

03 이 만든 건 **글의 구조**다. 무슨 글이 몇 편이고 어떻게 나뉘나.
**그런데 그건 사람이 보는 순서가 아니다.**

```
03 이 준 것        큰 갈래 안에 작은 갈래, 그 안에 글
   ↓
사람은 그 순서로 안 본다
   ↓
처음 온 사람은 홈에서 3초 안에 "이 사람이 뭐 하는 사람"인지 알아야 한다
   ↓
그 셋을 어느 화면 어느 칸에 놓을지 정하는 것          ← 여기가 04
   ↓
그 배치가 05 에서 검색 규칙이 되고 06 에서 실제 화면이 된다
```

화면 한 장이 세 군데로 그대로 간다.

| 어디서 | 화면 한 장이 뭐가 되나 |
|---|---|
| 05 기술문서 | 제목 · 설명 · 기계가 읽는 표시가 붙는 자리 |
| 06 사이트 | 실제로 지어지는 화면. 요소마다 다섯 층이 지시서다 |
| 07 발행 | 글이 들어갈 칸의 크기와 자리 |

### 화면을 다 그리지 않는 이유

읽는 쪽이 사람이다. **그게 앞 단계와 다른 점이다.**

| | |
|---|---|
| 03 위키 | 읽는 쪽이 AI 라 양이 늘어도 된다 |
| **04 화면설계서** | **읽는 쪽이 사람이다.** 04 통과 조건이 「개발 · 기획 몰라도 읽히나」다 |

**양이 많아서 문제가 아니라 화면에 닿기까지가 멀어서 문제다.**

---

## 산출물

**문서 3장.** 03 이 3장을 낸 것과 같은 무게다.

```
{작업폴더}/outputs/04-design/
├── plan.md              ★ 여기부터 본다. 지금 어디고 다음에 뭘 하나
├── 화면설계서.html       무엇을 만드나. 뼈대 · 화면 · 콘텐츠 배치
└── 근거.html            왜 그렇게 정했나. 디자인 값 · 안 하는 것 · 유추한 것
```

기계가 읽는 판은 따로 남는다.

| | |
|---|---|
| `.kb/화면자리.tsv` | 03 이 준 것 한 줄씩 · 어느 화면에 들어갔나 |
| `.kb/화면검사.json` | 자리 검사 결과 |

### md 화면설계서를 안 낸다

**두 벌을 내면 어긋난다.** 앞 회차에 실제로 그랬다.
`.md` 와 `.html` 을 둘 다 냈더니 `.md` 가 곧 낡았고, 사람이 그 파일 맨 위에
**「이 파일은 최신이 아닙니다」** 경고문을 손으로 붙여야 했다.

**경고문을 붙였다는 건 누군가 낡은 쪽을 볼 뻔했다는 뜻이다.**

| 왜 html 이 정본인가 | |
|---|---|
| 1 | **화면설계서는 그림이 본질이다.** 코드블록 안에 띄어쓰기로 그린 네모는 화면에서 다 무너진다 |
| 2 | **05·06 이 못 읽지 않는다.** 모델이 읽는 파이프라인에서 기계용 형식은 대개 필요 없다 |
| 3 | **읽을 사람이 사람이다.** 통과 조건이 「개발 · 기획 몰라도 읽히나」다 |

**`plan.md` 만 md 로 낸다.** 01 · 02 · 03 이 전부 그 규격이고 다음 단계가 제일 먼저 보는 자리다.

### 링크로 발행하지 않는다

**파일로만 낸다.** 화면설계서에는 이름 · 이력 · 근거 숫자 · 연락 칸이 들어간다.
개인정보 값은 안 적지만 **이 사람이 누구고 뭘 해왔는지는 그대로 들어 있다.**

| | |
|---|---|
| 1 | 이 체인은 **만드는 과정이 전부 비공개**다 |
| 2 | 링크로 내면 파일과 링크 **두 벌**이 생긴다. 위의 md · html 사고와 같은 계열이다 |
| 3 | 올리고 싶으면 **사람이 직접 하면 된다.** 04 가 알아서 올릴 자리가 아니다 |

### 안 만드는 것

| | 어디서 |
|---|---|
| 기계가 읽는 표시의 실제 값 · 주소 확정 · robots · 사이트맵 · 제목과 설명 문구 | 05 TECH |
| 실제 페이지 · 사이트 | 06 BUILD |
| 글 본문 | 07 PUBLISH |
| 차트 · 지표 화면 | 08 MEASURE |

---

## 절대 규칙

| | |
|---|---|
| 지어내기 | 03·02 에 없는 것을 화면에 올리지 않는다. **비면 비었다고 말한다** |
| 예시 | **앞사람 산출물에서 안 가져온다. 지어내고 지어냈다고 적는다** |
| 레퍼런스 | **값만 가져온다.** 그림을 산출물에 안 넣는다. 어디서 가져왔는지는 적는다 |
| 사진 | **화면설계서에 안 넣는다.** 미리보기는 전부 CSS 로 그린다 |
| 개인정보 | 화면설계서에 **이메일 · 전화 · 실명 값을 안 적는다.** 「연락 칸이 있다」까지만 |
| 절대경로 | 산출물에 안 적는다. 사용자 이름이 박혀 있다 |
| 개수 | **화면 장수 · 요소 개수 · 메뉴 개수를 미리 안 박는다.** 다 그리고 나서 말한다 |
| 클릭 | **다섯 개.** 실제 개수를 화면에 쓴다. 이것만 개수를 박는다 |
| 갈래와 메뉴 | **큰 갈래 수를 메뉴 수로 옮기지 않는다.** 아래 |
| 근거 없는 문장 | 02 가 **「근거가 없다」고 표시한 구절은 화면 카피로 안 쓴다** |
| 자동 커밋 | **스킬이 알아서 `git commit` 하지 않는다** |
| 발행 | **링크로 안 올린다.** 파일로만 낸다 |
| 어려운 말 | 안 쓴다. 써야 하면 그 자리에서 한 줄로 푼다 |
| 중간에 멈춤 | 지금까지 나온 것만으로 문서를 만든다. **완주를 조건으로 걸지 않는다** |
| 내부 문장 | 이 문서의 규칙을 화면에 읽어주지 않는다 |
| 대시와 불릿 | 화면에 내는 문구에 대시(—)와 하이픈 불릿을 쓰지 않는다. 표나 번호로 쓴다 |
| 경로 | 이 파일에 적어두지 않는다. 설정에서 읽거나 사용자에게 묻는다 |
| 시간 | **숫자로 약속하지 않는다** |

### 큰 갈래 수를 메뉴 수로 옮기지 않는다

**앞 회차에 여기서 걸렸다.** 주제 분류를 그대로 메뉴로 올렸더니
같은 소재가 서너 곳에 반복됐다. 판을 다시 짜야 했다.

> **메뉴는 사람의 동선이고 갈래는 주제 구조다. 둘을 겹치려고 하면 메뉴가 불어난다.**

03 도 같은 답에 따로 도달해서 `구조.md` 에 이렇게 적어 놨다.

> 이건 글을 나누는 칸이지 사이트 메뉴가 아니다.

**03 이 준 큰 갈래와 작은 갈래는 글 자리 안쪽 분류다.**
메뉴로 올리지 않는다. **메뉴 개수를 기본값으로 박지도 않는다.** 후보를 채워 내고 사람이 고른다.

---

## 대화 원칙

**상담이 아니라 작업이다.** 그런데 결과를 볼 때는 **재미있어야** 한다.

| ✕ 깎기 | ✕ 치켜세우기 |
|---|---|
| "자료가 좀 얇은데요" | "디자인 감각이 좋으시네요" |

둘 다 평가다. 대신 **형용사를 빼고 사실과 발견만** 내놓는다.

```
✕  "정말 멋진 사이트가 나올 것 같아요!"
○  "만드신 것 중 셋이 같은 화면 구조를 쓰고 있더라고요. 그래서 한 틀로 묶었습니다."
```

**칭찬보다 「이건 안 하는 게 낫다」를 먼저 말한다.**
04 는 사람이 고를 게 많은 단계라 여기서 아첨하면 고르는 근거가 없어진다.

**03 을 막 끝낸 사람이다.** 03·02 에서 이미 답한 것을 다시 묻지 않는다.
컨셉 · 읽을 사람 · 말투 · 표기는 `컨셉.md` 에, 갈래와 글 목록은 `구조.md` 에 있다. **읽고 쓴다.**

---

## 진행 표시

클릭 질문마다 앞에 `[3/5]` 를 붙인다. **그게 전부다.**
"조금만 더 힘내세요" 같은 말은 넣지 않는다.

### 클릭 질문은 반드시 하나씩

**질문을 두 개 이상 한 창에 넣지 않는다.** 탭이 생기고, 탭을 다 채워야 제출된다.

| | |
|---|---|
| 1 | 질문 하나면 탭이 없고 고르는 즉시 넘어간다 |
| 2 | 보기는 2~4개, 각각 한 줄 설명 |
| 3 | **빠져나갈 보기를 하나 둔다.** "잘 모르겠다" · "알아서 정해줘" |
| 4 | 후보 비교가 필요하면 preview 를 쓴다. **디자인 후보가 그 자리다** |

### 서술로 받는 것

셋뿐이다. **클릭 개수에 안 센다.**

| | |
|---|---|
| 1 | 좋아하는 사이트 링크나 이미지 · 정해둔 색과 글꼴 (클릭 ① 뒤) |
| 2 | "이건 아닌데" (「만든 것」 표 뒤) |
| 3 | "고칠 데" (`plan.md` 뒤) |

**클릭 다섯 개가 전부 「고르기」이지 「하기」가 아니다.** 사람은 판단만 하고 손일은 안 한다.

### 손일을 사람에게 넘기지 않는다

**「만든 것」 목록을 사람에게 적어 오라고 하지 않는다.**
02 근거와 03 위키와 이미지에 이미 다 있다. **04 가 다 캐서 표로 채워 놓고 사람은 「이건 아닌데」만 짚는다.**

산출물에 이 세 말이 적히는 자리가 있으면 거기가 안 될 자리다.

```
사용자가 직접   ·   나중에   ·   필요하면
```

---

## STEP 1 · 첫 메시지

시작하자마자 여섯 가지를 말한다.
에셋엔진이 뭔지, 이 단계가 그중 어디인지, 뭘 하는 건지, **여기서 안 하는 게 뭔지**,
어떻게 가는지, **미리 알아둘 게 뭔지**.

**아래를 마크다운 그대로 낸다.** 코드블록으로 감싸지 않는다. 감싸면 표가 안 먹고 뭉쳐서 안 읽힌다.
선이나 네모로 모양내지 않는다. 띄어쓰기로 만든 정렬은 화면에서 다 무너진다.

**낸 뒤 멈춘다.** 곧바로 질문 창을 띄우지 않는다. 창이 뜨면 안내문이 가려진다.

내는 내용은 이것이다.

---

## 04 DESIGN · 화면 그리기

### 에셋엔진이 뭐냐면

한 일을 **꺼내고, 성과의 언어로 옮기고, 하나의 구조로 이어**
검색과 AI 답변에서 발견되게 만드는 워크플로우입니다.

단계마다 스킬 하나씩이고, 끝나면 다음 스킬을 알려드립니다.
**한 번에 하나만 아시면 됩니다.**

| | |
|---|---|
| 01 재료화 | 흩어진 자료를 한 형식으로 모았습니다 |
| 02 컨셉 잡기 | 반복해오신 일을 찾고 컨셉 한 줄을 정했습니다 |
| 03 구조 잡기 | 주제를 묶고 글 목록을 뽑았습니다 |
| **04 화면설계** | **지금 여기** |
| 05 기술 문서 | 검색·AI 에 걸리게 하는 규칙을 정합니다 |
| 06 사이트 짓기 | 실제로 짓고 잘 됐는지 검사합니다 |
| 07 어드민 · 발행 | 관리할 자리를 만들고 첫 글을 냅니다 |
| 08 지표 | 효과를 재고, 다시 07 로 돌아갑니다 |

### 이 단계가 뭐냐면

03 에서 **글의 구조**를 만드셨습니다. 무슨 글이 몇 편이고 어떻게 나뉘는지.

그런데 **사람은 그 순서로 안 봅니다.**

처음 온 사람은 홈에서 **3초 안에** "이 사람이 뭐 하는 사람"인지 알아야 합니다.
그다음에야 글로 들어갑니다.

**여기서는 그걸 어느 화면 어느 칸에 놓을지 정합니다.**
화면이 몇 종류고, 메뉴를 어떻게 짜고, 무슨 색과 글꼴을 쓰는지까지요.

### 그게 어디에 쓰이냐면

| | 여기서 그린 화면 한 장이 |
|---|---|
| 05 기술 문서 | 제목과 설명이 붙는 자리가 됩니다 |
| 06 사이트 짓기 | **그대로 실제 화면이 됩니다.** 요소마다 만드는 순서가 적혀 있어요 |
| 07 발행 | 글이 들어갈 칸의 크기와 자리가 됩니다 |

### 여기서 안 하는 게 있습니다

| | |
|---|---|
| **사이트를 짓지 않습니다** | 06 에서 합니다. 여기서는 **그림과 설명**까지 |
| **글을 쓰지 않습니다** | 07 에서 씁니다 |
| **주소를 확정하지 않습니다** | 05 에서 정합니다. 03 이 후보 둘을 냈습니다 |
| **검색에 걸리는 규칙을 안 정합니다** | 05 에서 합니다. 여기서는 **자리만** 표시합니다 |

### 어떻게 가냐면

질문 다섯 개면 끝납니다. 그사이에 제가 화면을 그리는 시간이 좀 걸립니다.

| | | |
|---|---|---|
| 1 | 03 에서 정하신 구조를 읽습니다 | 기다리시면 됩니다 |
| 2 | **디자인을 어디서 가져올지** 고르기 | 질문 1개 |
| 3 | **뽑은 값을 보여드림** | 질문 1개 |
| 4 | **화면과 메뉴 후보를 드림** | 질문 1개 |
| 5 | **만든 것 목록을 캐서 보여드림** | 질문 1개 |
| 6 | 연락 칸을 둘지 | 질문 1개 |
| 7 | 화면을 그립니다 | 기다리시면 됩니다 |

중간에 멈추셔도 되고, 모르는 건 안 정하고 넘어가셔도 됩니다.

### 끝나면 뭘 받냐면

**문서 3장**입니다.

| | |
|---|---|
| `화면설계서.html` | **두 번 누르면 브라우저에서 열립니다.** 화면 그림과 설명 |
| `근거.html` | 왜 그렇게 정했는지. 색과 글꼴 값 · 안 하기로 한 것 |
| `plan.md` | 지금 어디까지 정해졌고 다음에 뭘 하나 |

### 미리 알려드릴 것

| | |
|---|---|
| **화면** | 글 한 편마다 그림을 그리지 않습니다. **같은 모양으로 나오는 것은 한 장으로 묶습니다.** 몇 개가 그 모양을 쓰는지 같이 적습니다 |
| **장수** | 몇 장을 그리겠다고 미리 약속하지 않습니다. **다 그리고 나서 몇 장인지 알려드립니다** |
| **글** | 지금은 글이 0편입니다. 여기서 그리는 건 **글이 들어갈 자리**입니다. 글은 07 에서 씁니다 |
| **디자인** | 색과 글꼴까지 정합니다. **다만 실제로 어떻게 보이는지는 06 에서 지어봐야 압니다** |
| **레퍼런스** | 좋아하는 사이트를 주시면 **거기서 값만 가져옵니다.** 그림은 안 가져옵니다 |
| **파일** | 인터넷에 안 올립니다. **컴퓨터 안에 파일로만 만듭니다** |
| **연락처** | 화면에 연락 칸을 둘지는 여쭤봅니다. **값은 문서에 안 적습니다** |

---

**지금 다 이해 안 되셔도 됩니다.** 끝까지만 오시면 문서가 남고,
그 순서대로 하나씩 하시면 됩니다.

다 읽으셨으면 **아무 말이나 보내주세요.** 바로 시작할게요.
궁금한 게 있으면 먼저 물어보셔도 됩니다.

---

**이 안내는 한 번만 낸다.** 사용자가 답하면 바로 STEP 2 로 간다.
넘어갈 때 안내를 다시 요약하지 않는다. 중간에도 다시 요약하지 않는다.

---

## STEP 2 · 03·02 산출물 읽기

**화면에 한 줄만 낸다.** 읽은 내용을 보고하지 않는다.

### 설정을 조용히 읽는다

```bash
cat ~/.claude/iny-config.json
```

`kb_path` 를 꺼내 작업 폴더로 쓴다. `~` 는 실제 홈 경로로 바꾼다.
파일이 없으면 **작업 폴더가 어디인지 묻는다.** 짐작해서 뒤지지 않는다.

### 읽는 것

```
{작업폴더}/outputs/03-structure/plan.md
{작업폴더}/outputs/03-structure/구조.md
{작업폴더}/outputs/03-structure/분류.md
{작업폴더}/outputs/02-identify/컨셉.md
{작업폴더}/outputs/02-identify/근거.md
{작업폴더}/wiki/index.md
{작업폴더}/wiki/concepts/            ★ 전부
```

**03 이 넘긴 것을 다 읽는다.** 04 는 앞 세 단계와 달리 입력이 크지 않다.
03 이 이미 좁혀준 결과가 04 의 입력이다.

**「전부 다 읽는다」고 화면에 약속하지는 않는다.** 재료가 큰 사람에게는 03 이 더 많이 넘길 수 있다.
말할 때는 **「03 이 넘긴 것을 읽었다」**로 적는다. 조건이 걸린 약속이 된다.

### 이미지도 읽는다

03 이 **재료 이미지 안에 「화면에 보이는 것」을 적어 뒀다.** 본인이 만든 화면들이다.

**「만든 것」 화면에 뭘 걸지가 여기서 나온다.** STEP 6 에서 쓴다.
본문이 채워진 이미지 재료를 훑어 목록으로 잡아 둔다. **화면에는 안 낸다.**

### 없으면 여기서 멈춘다

**03 구조 문서가 없으면 04 가 아예 못 선다.** 그릴 것이 없기 때문이다.

```
03 에서 만든 구조 문서가 안 보입니다.

04 는 03 이 정한 구조를 화면으로 옮기는 단계라
03 을 먼저 하셔야 합니다.

  https://github.com/iny101400-young/iny-structure

받으시려면 이 폴더에서 아래를 그대로 치시면 됩니다.

  git clone https://github.com/iny101400-young/iny-structure.git
  cd iny-structure
  bash install.sh

깔고 나서 Claude Code 를 다시 열고 "구조 잡기" 라고 치세요.
```

`구조.md` 는 있는데 `근거.md` 가 없으면 **멈추지 않고 간다.**
다만 「근거 문서가 없어서 만든 것 목록이 얇습니다」를 `plan.md` 에 적는다.

### 무엇을 어디에 쓰는지

| 앞 단계가 준 것 | 04 가 쓰는 자리 |
|---|---|
| 02 **컨셉 한 줄** | **첫 화면 문장.** 아래 「여러 줄일 때」를 본다 |
| 02 **소개 한 줄** | 짧은 판. 이미 압축돼 있다 |
| 02 **표기 네 칸** | 이름이 화면에 나가는 모양 |
| 02 **말투** (쓰는 문장 · 안 쓰는 문장) | **화면 문구를 이걸로 쓴다** |
| 02 **읽을 사람** 여러 종류 | **STEP 9 검토의 기준** |
| 02 **근거 숫자** | 홈에 큰 숫자로 걸 것 |
| 02 **재명명 표** | 화면 제목과 메뉴 이름이 이 말로 붙는다 |
| 02 **안 할 것** | `근거.html` 의 「안 하는 것」 |
| 02 **엔티티** (흩어진 계정 주소) | 자리만 잡는다. 값과 코드는 05 |
| 03 **큰 갈래 · 작은 갈래** | **글 자리 안쪽 분류.** 메뉴로 안 올린다 |
| 03 **글 목록 초안** | 「글 한 편」 화면이 몇 개를 덮는지 |
| 03 **비어 있는 것** | **글이 0편일 때의 화면**을 그릴 근거 |
| 03 **답하는 질문** | 어느 화면이 그 질문에 답하나 |
| 03 **주소 후보 둘** | ★ **04 가 고르지 않는다. 05 다** |
| 03 **갈래별 검색어** | ★ **04 가 안 쓴다. 05 다** |
| `wiki/concepts/` | 「글 한 편」 화면의 뼈대 |
| 이미지 재료 | 「만든 것」 목록의 근거 |

### 컨셉 한 줄이 여러 줄이면

**「한 줄」이라고 부르지만 여러 줄일 수 있다.** 사용자가 직접 쓴 문장인 경우가 많다.

**첫 화면에 여러 줄을 다 넣으면 화면이 안 선다.**
02 의 **「소개 한 줄」이 이미 압축본**이다. 그걸 쓴다.

**어느 줄을 어느 화면에 쓸지 04 가 갈라 배치하고 그 표를 `근거.html` 에 낸다.**
사용자가 쓴 말을 요약해서 고치지 않는다. **자르지 말고 나눠서 놓는다.**

### 02 가 근거 없다고 표시한 구절

02 는 스스로 「이 문장은 재료가 뒷받침하지 않는다」고 표시한 자리가 있을 수 있다.
03 도 그런 구절을 분류 기준에서 뺐다.

> **04 도 그 구절을 화면 카피로 쓰지 않는다.**
> 근거 없는 문장을 첫 화면에 걸면 그게 사이트 전체의 첫인상이 된다.

**뺐다는 것을 `근거.html` 에 한 줄 적는다.** 조용히 빼지 않는다.

### 화면에는 이 한 줄만

```
03 에서 정하신 구조를 읽었어요.

  큰 갈래 {N}개 · 작은 갈래 {N}개 · 글 {N}편

이제 이걸 화면에 어떻게 놓을지 정하겠습니다.
```

---

## STEP 3 · 디자인을 어디서 가져올까

**04 의 첫 질문이다.** 02 가 이걸 일부러 안 물었다.
「04 가 같은 걸 더 깊게 묻는다. 두 번 묻는 게 정신없음의 원인이었다」고 적혀 있다.

**디자인이 먼저 정해져야 미리보기를 진짜 색으로 그린다. 그래야 화면을 한 번만 그린다.**

### 화면에는 이렇게 나간다

```
화면을 그리기 전에 색과 글꼴부터 정합니다.
이게 정해져야 미리보기를 진짜 색으로 그려서 보여드릴 수 있어요.
```

**[1/5] 디자인을 어디서 가져올까요?** (단일 선택)

| 보기 | 설명 |
|---|---|
| `좋아하는 사이트가 있다` | `주소나 화면 캡처를 주시면 거기서 값만 가져옵니다` |
| `정해둔 색이나 글꼴이 있다` | `쓰시던 값이 있으면 그대로 씁니다` |
| `없다. 요즘 사례를 찾아서 후보를 만들어 달라` | `비슷한 분야 사이트를 찾아보고 후보를 만듭니다` |
| `없다. 찾지 말고 후보를 만들어 달라` | `인터넷을 안 보고 만듭니다. 인터넷이 막힌 곳에서도 됩니다` |

**웹 검색 여부를 이 질문 안에서 가른다.** 따로 묻지 않는다.
따로 물으면 클릭이 하나 늘고, 갈래마다 클릭 개수가 달라져서 진행 표시가 안 맞는다.

### 검색을 쓰기로 하면

**횟수를 약속하지 않는다.** 몇 번 찾겠다고 화면에 말하지 않는다.

| | |
|---|---|
| 찾는 것 | **값**이다. 색 · 글꼴 · 간격 · 굵기 |
| 안 가져오는 것 | **문장.** 남의 사이트 카피를 우리 화면에 안 옮긴다 |
| 적는 것 | **어디를 봤는지 `근거.html` 에 남긴다** |

**검색이 안 되면 멈추지 않는다.** 「인터넷을 못 봤습니다」를 한 줄 말하고
안 찾는 쪽으로 그대로 간다. **여기서 스킬이 멈추면 안 된다.**

### 후보를 만들 때

[`references/디자인규칙.md`](references/디자인규칙.md) 를 읽고 따른다.
**거기에 「후보가 서로 비슷해지는 것을 막는 목록」이 있다.**

이 목록을 안 보고 후보를 세 개 내면 **셋 다 같은 데로 몰린다.**

---

## STEP 4 · 값을 뽑아 되보여준다

**두 갈래가 여기서 만난다.** 받은 것이 있으면 값을 뽑고, 없으면 후보를 만든다.

### 사이트나 이미지를 받았으면

**값만 뽑는다.** [`references/디자인규칙.md`](references/디자인규칙.md) 의 「값 뽑기」 절대로 한다.

| 뽑는 것 | |
|---|---|
| 색 | 4~6개. 각각 어디에 쓸지 이름을 붙인다 |
| 글꼴 | 역할 둘 이상. 제목용 · 본문용 |
| 간격 | 되풀이 쓰는 값 몇 개 |
| 굵기 · 모서리 · 움직임 | 있으면 |

**그림을 저장하지 않는다.** 받은 이미지 파일을 산출물 폴더로 복사하지 않는다.

### 받은 게 없으면

**후보를 채워서 낸다. 백지에 묻지 않는다.**

후보마다 **왜 이게 이 사람에게 맞는지 한 줄**을 붙인다.
02 컨셉과 읽을 사람에서 근거를 끌어온다. 취향으로 고르게 하지 않는다.

### 화면에는 이렇게 나간다

`AskUserQuestion` 의 preview 를 쓴다. **디자인은 글로 설명하면 안 고른다.**

```
색과 글꼴을 이렇게 잡아봤어요.

  {이름}
      배경 {값}   글자 {값}   포인트 {값}
      제목 {글꼴}   본문 {글꼴}
      {왜 이게 맞는지 한 줄}
```

**[2/5] 이 디자인으로 갈까요?** (단일 선택)

| 보기 | 설명 |
|---|---|
| `{후보 이름}으로 가자` | `이 값으로 화면을 그립니다` |
| `{다른 후보 이름}으로 가자` | `이 값으로 화면을 그립니다` |
| `일부만 바꾸고 싶다` | `어느 걸 뭐로 바꿀지 말씀해 주세요` |
| `잘 모르겠다` | `첫 번째로 가고 06 에서 실제 화면을 보고 바꿀 수 있습니다` |

**「잘 모르겠다」의 설명이 중요하다.** 디자인은 지어봐야 안다.
**여기서 못 정해도 뒤가 안 막힌다**는 걸 말해준다.

### 정한 값을 어디에 적나

| 어디 | 무엇 |
|---|---|
| `근거.html` | **값 전부.** 색 코드 · 글꼴 이름 · 간격 · 어디서 가져왔나 |
| `화면설계서.html` | **쓴 결과.** 미리보기가 그 색으로 그려져 있다 |

**같은 값을 두 파일에 적지 않는다.** 어긋난다.

---

## STEP 5 · 화면 종류와 메뉴

**여기가 04 의 중심이다.** 03 이 명시적으로 넘긴 자리다.

### 백지에 묻지 않는다

**후보를 다 채워서 낸다.** 그리고 고치게 한다.

화면 종류를 잡는 순서는 이렇다.

| | |
|---|---|
| 1 | **03 이 준 것을 전부 늘어놓는다.** 큰 갈래 · 작은 갈래 · 글 · 비어 있는 것 |
| 2 | **04 가 더할 것을 찾는다.** 홈 · 만든 것 · 소개 같은 것. 02 근거에서 나온다 |
| 3 | **같은 틀로 나오는 것을 묶는다.** 글 여러 편이 한 틀이면 화면 한 장 |
| 4 | **어디에도 자리가 없는 03 산출물이 있는지 본다.** 있으면 화면을 더한다 |

**화면 개수를 미리 안 박는다.** 03 이 준 것이 붙는 만큼 정해진다.

### 메뉴는 따로 짠다

**화면 종류가 곧 메뉴가 아니다.**

```
화면 종류    문서에 그릴 그림의 개수
메뉴         사이트 위쪽에 실제로 걸리는 것
```

메뉴는 **사람의 동선**이다. 화면이 열 종류여도 메뉴는 서넛일 수 있다.

**후보를 둘 이상 낸다.** 하나만 내면 고르는 게 아니라 승인하는 것이 된다.
후보마다 **무엇을 앞에 세우는가**가 달라야 한다.

```
가   만든 것을 앞에 세운다        무엇을 만들 줄 아는지 먼저 보여준다
나   글을 앞에 세운다             어떻게 생각하는지 먼저 보여준다
```

**이건 지어낸 예시다.** 실제 후보는 그 사람의 02 컨셉과 읽을 사람에서 나온다.

### 화면에는 이렇게 나간다

```
화면을 이렇게 나눠봤어요.

  {화면 이름}          {이 화면이 덮는 것}
  {화면 이름}          {이 화면이 덮는 것}
  {화면 이름}          글 {N}편이 이 한 틀을 씁니다
  {화면 이름}          갈래 {N}개가 이 한 틀을 씁니다

메뉴는 두 가지로 짜봤어요.

  가   {메뉴} · {메뉴} · {메뉴}
       {무엇을 앞에 세우나 한 줄}

  나   {메뉴} · {메뉴} · {메뉴}
       {무엇을 앞에 세우나 한 줄}

03 에서 정하신 큰 갈래 {N}개는 메뉴로 안 올렸어요.
그건 글 안에서 나누는 칸이라, 메뉴로 올리면 같은 글이 여러 메뉴에 겹쳐 나옵니다.
```

**맨 아래 두 줄을 반드시 넣는다.** 03 산출물이 메뉴에 안 보이면
사용자는 「내 갈래가 어디 갔지」 한다. **왜 안 올렸는지 그 자리에서 말한다.**

**[3/5] 메뉴를 어떻게 짤까요?** (단일 선택)

| 보기 | 설명 |
|---|---|
| `가로 가자` | `{무엇을 앞에 세우나}` |
| `나로 가자` | `{무엇을 앞에 세우나}` |
| `메뉴 이름을 고치고 싶다` | `어느 걸 뭐로 바꿀지 말씀해 주세요` |
| `잘 모르겠다` | `가로 가고 06 에서 다시 볼 수 있습니다` |

### 글이 0편일 때의 화면

**지금 있는 건 글이 아니라 뼈대다.** 03 이 만든 문서는 글의 재료지 발행된 글이 아니다.

**목록 화면이 비어 있을 때 뭐가 보이는지 같이 그린다.**
안 그리면 06 이 지었을 때 빈 화면이 나오고, 그때 다시 04 로 돌아와야 한다.

| 그릴 것 | |
|---|---|
| 글이 0편인 목록 화면 | 뭐라고 적혀 있나 |
| 글이 한두 편인 목록 화면 | 칸이 안 무너지나 |
| 아직 문서가 없는 갈래 | 03 이 「비어 있는 것」으로 표시한 자리다 |

---

## STEP 6 · 「만든 것」 목록

**03 산출물에 이 칸이 없다.** 03 은 **글**만 냈다.

| | |
|---|---|
| 03 이 준 것 | **일**의 이름. 「무엇을 어떻게 했다」 |
| 04 가 필요한 것 | **물건**의 이름. 언제 만들었고 지금 볼 수 있나 |

**03 에 더 달라고 하지 않는다.** 03 의 일이 아니다. **04 가 캔다.**

### 어디서 캐나

| | |
|---|---|
| 02 `근거.md` | 「만든 일」이 적힌 절 |
| `wiki/concepts/` | 문서 안의 「무엇을」 절 |
| **이미지 재료** | ★ **03 이 「화면에 보이는 것」을 적어 뒀다.** 본인이 만든 화면들이다 |

**이미지가 여기서 값을 한다.** 03 이 이미지를 자동으로 안 뺀 판단이 04 에서 또 값을 한다.

### 다 채워 놓고 짚게 한다

**사람에게 적어 오라고 하지 않는다.** 04 가 표를 다 채운다.

| 채우는 칸 | |
|---|---|
| 이름 | 물건의 이름 |
| 무엇 | 한 줄 |
| 언제 | 재료에 날짜가 있으면 |
| 지금 볼 수 있나 | 링크가 있으면. **없으면 「없음」이라고 적는다** |
| 어디서 나왔나 | 어느 재료에서 캤는지 |

**빈칸과 「없음」은 다르다.** 빈칸은 안 정한 것이고 「없음」은 정한 것이다.
**못 찾은 칸은 「재료에 없음」이라고 적는다.** 비워 두지 않는다.

### 화면에는 이렇게 나간다

```
만드신 것을 자료에서 캐서 표로 만들었어요.
{N}개 찾았습니다. 아닌 게 있으면 빼시면 됩니다.

  {이름}      {무엇}                {언제}   {지금 볼 수 있나}
  {이름}      {무엇}                {언제}   {지금 볼 수 있나}

이 중 {N}개는 만드신 화면 이미지에서 찾았어요.
```

**[4/5] 이 목록이 맞나요?** (단일 선택)

| 보기 | 설명 |
|---|---|
| `맞다` | `이대로 화면에 넣습니다` |
| `뺄 게 있다` | `어느 건지 말씀해 주세요` |
| `빠진 게 있다` | `이름만 말씀해 주시면 자료에서 찾아 채웁니다` |
| `이 화면을 안 만들겠다` | `만든 것 화면을 빼고 글 중심으로 갑니다` |

**「빠진 게 있다」의 설명이 중요하다.** 이름만 말하면 04 가 나머지를 채운다.
**사람이 표를 채우게 하지 않는다.**

---

## STEP 7 · 연락 칸

**02 가 「하단에 무엇을 노출할지는 나중에 고른다」로 미뤄 둔 자리다.**

### 값을 묻지 않는다

**「칸을 둘지」만 묻는다.**

| | |
|---|---|
| 04 가 정하는 것 | **연락 칸이 있나 없나. 어느 화면 어디에 있나** |
| 04 가 안 정하는 것 | **거기 뭐가 적히나.** 이메일 주소 · 전화번호 · 실명 |

**화면설계서에 연락처 값을 안 적는다.** 「연락 칸이 있다」까지만 적는다.
값은 06 이 지을 때 사람이 직접 넣는다.

이 문서는 파일로만 남지만, **06 을 거치면 사이트가 되고 07 이 발행한다.**
**한 번 들어간 연락처는 그대로 공개된다.**

**[5/5] 연락할 자리를 둘까요?** (단일 선택)

| 보기 | 설명 |
|---|---|
| `둔다` | `자리만 잡습니다. 뭘 적을지는 06 에서 직접 넣으십니다` |
| `계정 링크만 둔다` | `연락처 대신 계정 주소만 겁니다` |
| `안 둔다` | `연락 칸 없이 갑니다. 나중에 06 에서 더할 수 있습니다` |
| `잘 모르겠다` | `계정 링크만 두고 06 에서 다시 볼 수 있습니다` |

---

## STEP 8 · 화면 그리기

**화면에는 한 줄만 낸다.** 그리는 동안 진행 상황을 쏟지 않는다.

```
화면을 그릴게요. 조금 걸립니다.
```

### 화면 한 장에 적는 차례

**이걸 「층」이라고 부르지 않는다.** 층은 아래 요소 쪼개기에만 쓴다.
이름이 겹치면 읽는 사람이 둘을 같은 것으로 본다.

| | |
|---|---|
| 1 | **화면 이름** (번호 + 한글 제목) |
| 2 | **목적** 한 줄. 이 화면이 왜 있나 |
| 3 | **미리보기** ← 왼쪽 칸. **글로 안 적고 그린다** |
| 4 | **요소들** ← 오른쪽 칸. 왼쪽 번호와 맞춘다 |
| 5 | **규칙** |
| 6 | **폰에서 볼 때** |

**3번 자리를 글로 적지 않는다.** 앞 회차에 이 칸이 「무엇을 어떤 순서로 쌓나」라는
글이었는데, 사람이 다시 만들 때 그림으로 바꿨다. **그림으로 바꾼 게 그 문서의 값어치를 만들었다.**

### 화면 이름표

**번호 + 한글 제목**이다.

```
04  기록 목록          ← 지어낸 예시입니다
```

| | |
|---|---|
| 번호 | **화면이 문서에 나오는 차례.** 글 번호가 아니다 |
| 한글 제목 | 화면 이름 그대로 |

**영문 약자를 만들지 않는다.** 한글 화면 이름을 영어로 옮기면
그 영어가 사람마다 달라지고, 그러면 05·06 이 못 따라간다.

**번호를 안 민다.** 화면을 빼도 뒤 번호를 당기지 않고 구멍으로 남긴다.
당기면 05·06 이 가리키던 것이 어긋난다.

**주소가 아니다.** 주소는 05 가 정한다. 화면 이름표는 문서 안에서만 쓴다.

### 요소 한 개를 다섯 층으로 쪼갠다

`UI` `Logic` `Data` `API` `Deploy`. **이건 진짜로 층이다.**
화면 → 규칙 → 저장 → 바깥 → 배포로 내려간다.

**「없음」을 적는다.** 개인 사이트는 `Logic` 과 `API` 가 대부분 없음이 정상이다.
**빈칸과 「없음」은 다르다.** 빈칸은 안 정한 것이고 「없음」은 정한 것이다.

**요소 개수를 안 박는다.** 화면마다 다르다.

### 어느 화면에 기계가 읽는 표시가 붙나

**자리만 표시한다.** 값은 05 가 정한다.

```
○  이 화면에는 기계가 읽는 표시가 붙습니다        ← 04 가 여기까지
✕  Article 스키마에 headline 은 이 값으로         ← 05 가 한다
```

**★ 여기가 04 의 제일 큰 유혹이다.**
검색과 AI 답변에 걸리게 하는 장치를 04 가 다 정해버리면 **05 가 할 게 없어진다.**
04 는 「이 화면에 질문형 제목이 온다」 「여기에 표시가 붙는다」까지만 적는다.

### 만드는 규칙

[`references/화면설계서.md`](references/화면설계서.md) 와
[`references/화면설계서-템플릿.html`](references/화면설계서-템플릿.html) 을 읽고 따른다.

| | |
|---|---|
| 목차 띠 | 화면이 여러 장이면 맨 위에 넣는다. **없으면 못 찾는다** |
| 미리보기 | **정한 색과 정한 글꼴로 그린다.** 회색 빈 상자로 두지 않는다 |
| 사진 | **안 넣는다.** 전부 CSS 로 그린다 |
| 화면 늘리기 | 템플릿의 `.screen` 한 덩어리를 복사한다 |
| 다크모드 | 템플릿이 이미 대응한다. 지우지 않는다 |
| `word-break` | 템플릿의 `keep-all` 을 **지우지 않는다.** 지우면 한글이 낱말 가운데서 잘린다 |

### 근거.html 에 들어가는 것

**화면설계서에서 이걸 빼는 게 이 단계의 핵심이다.**

| | |
|---|---|
| 디자인 값 | 색 · 글꼴 · 간격 전부 |
| 어디서 가져왔나 | 레퍼런스 출처 |
| 왜 이 메뉴인가 | 후보 둘 중 왜 이걸 골랐나 |
| 컨셉 한 줄을 어떻게 갈랐나 | 어느 줄이 어느 화면에 갔나 |
| 안 하는 것 | 02 「안 할 것」 + 04 가 안 하기로 한 것 |
| 유추한 것 | 재료에 없어서 04 가 채운 것. **표시해 둔다** |
| 뺀 것 | 근거 없다고 표시된 구절 · 못 그린 화면 |

---

## STEP 9 · 검토하고 고친다

### 9-1. 읽을 사람으로 본다

**02 가 읽을 사람을 여러 종류로 갈라 놨고 어떤 검색어로 오는지까지 적어 놨다.**
**그 사람들을 그대로 세워서 화면을 본다.**

| | |
|---|---|
| 한 번에 한 종류씩 | 서로의 답을 뭉개지 않는다 |
| 묻는 것 | **「이 사람이 이 화면에서 뭘 못 찾나」** |
| 지어내지 않는다 | 02 에 적힌 것 밖으로 안 나간다. 모르면 모른다고 한다 |
| 아첨하지 않는다 | **「좋다」보다 「여기서 막힌다」를 먼저 찾는다** |

> **이건 실제 사용자 테스트가 아니다.** `근거.html` 에 그 한 줄을 적는다.
> 실제 사람에게 보여주는 건 06 이 지은 뒤에 한다.

**바깥 스킬을 부르지 않는다.** 합성 인구통계로 만든 사람보다
02 가 재료에서 뽑은 읽을 사람이 정확하다. 이 사이트의 실제 독자이기 때문이다.

**고친 것을 `근거.html` 에 남긴다.** 화면에는 개수와 종류만 낸다.

### 9-2. 자리 검사

**03 이 준 것 중 어느 화면에도 안 들어간 것이 있는지 기계로 잰다.**
눈으로 세면 매번 다르게 센다.

```bash
python3 "{이 스킬이 설치된 폴더}/scripts/자리검사.py" "{작업폴더}" --목록
```

03 산출물을 훑어 `.kb/화면자리.tsv` 를 만든다. 한 줄이 하나다.
큰 갈래 · 작은 갈래 · 글 제목 초안 · 비어 있는 것 · 위키 문서.

**04 가 `화면` 칸을 채운다.** 어느 화면 번호에 들어갔는지 적는다.

```bash
python3 "{이 스킬이 설치된 폴더}/scripts/자리검사.py" "{작업폴더}" --검사
```

빈 칸이 남아 있으면 짚어준다. **0 이 될 때까지 화면을 고친다.**

**자리가 없는 게 나오면 화면을 더하거나, 왜 안 넣었는지 `근거.html` 에 적는다.**
조용히 빼지 않는다. **03 이 준 것을 04 가 말없이 버리지 않는다.**

### 파이썬이 없거나 스크립트가 안 돌면

직접 센다. 다만 **한글 파일명을 NFC 로 맞추고 나서** `grep` 한다.

```python
import unicodedata
p = unicodedata.normalize('NFC', p)
```

01 과 03 이 이걸 안 해서 필터가 통째로 헛돈 적이 있다.
`grep -c "글자"` 가 0인데 `ls` 로는 그 경로가 보이면 이 문제다.

**스크립트가 안 돌아도 멈추지 않는다.** 직접 세고 **직접 셌다고 `plan.md` 에 적는다.**

---

## STEP 10 · 문서 쓰기 · 마무리

**여기가 마지막이다.** 아래를 **한 덩어리로** 낸다. 사이에서 멈추지 않는다.

쓰기 전에 한 줄 알리고 **답은 안 기다린다.**

```
정리해서 문서로 쓸게요.
```

쓰는 곳은 `{작업폴더}/outputs/04-design/` 다.
**이미 내용이 있으면 덮어쓰지 않고 묻는다.**

> 여기 지난번에 만든 게 있어요. 덮어쓸까요, 옆에 새로 만들까요?

### plan.md

```markdown
# {표시 이름} · 지금 어디고 다음에 뭘 하나
{날짜} · 04 DESIGN · 화면 {N}종류 · 요소 {N}개

## 컨셉 한 줄
{02 에서 정한 것 그대로}

## 정해진 것
| | 정해짐 | 어디에 |
|---|---|---|
| 화면 {N}종류 | ○ | 화면설계서.html |
| 메뉴 구성 | ○ | 화면설계서.html |
| 색 · 글꼴 · 간격 | ○ | 근거.html |
| 요소마다 다섯 층 | ○ | 화면설계서.html |
| 만든 것 {N}개 | ○ | 화면설계서.html |
| 연락 칸 | ○ | 화면설계서.html |
| 기계가 읽는 표시가 붙을 자리 | ○ | 화면설계서.html. **값은 05** |

## 안 정해진 것
| 무엇 | 왜 비었나 | 언제 정하면 되나 |
|---|---|---|
| 주소를 어떻게 만들지 | 03 이 후보 둘을 냈다 | 05 TECH |
| 기계가 읽는 표시의 값 | 04 는 자리만 잡았다 | 05 TECH |
| 실제 화면이 어떻게 보이나 | 지어봐야 안다 | 06 BUILD |
| 연락 칸에 뭘 적을지 | 값은 문서에 안 적는다 | 06 BUILD |
| {그 밖} | {왜} | {언제} |

## 화면 한 장이 몇 개를 덮나
| 화면 | 덮는 것 |
|---|---|
| {화면 이름} | 글 {N}편 |
| {화면 이름} | 갈래 {N}개 |

## 안 그린 것
| 무엇 | 왜 |
|---|---|
| {화면} | {왜} |

03 에서 넘어온 것은 전부 어느 화면엔가 자리를 가졌습니다.

## 다음에 할 일
1. {사람이 할 일}
2. {사람이 할 일}

## 다음 단계로 넘길 것
| 어디로 | 무엇 |
|---|---|
| 05 TECH | 화면 {N}종류 · 기계가 읽는 표시가 붙을 자리 · 03 이 낸 주소 후보 둘 · 갈래별 검색어 |
| 06 BUILD | `화면설계서.html` 통째로. 요소마다 다섯 층이 지시서다 |
| 07 PUBLISH | 글이 들어갈 자리와 칸 크기 |
| 08 MEASURE | 첫 화면에 무겁게 넣은 것이 있으면 그 값 |

## 05 로 가려면
git clone https://github.com/iny101400-young/iny-tech.git
cd iny-tech && bash install.sh
```

**「다음에 할 일」에 이 스킬이 할 일을 적지 않는다.** 거기 적히는 건 사람이 나중에 할 일이다.

### 다 쓰고 나서 · 여기서 멈추지 않는다

**아래를 이어서 바로 낸다.** `plan.md` 를 보여주고 답을 기다리지 않는다.

먼저 `plan.md` 내용을 채팅창에 그대로 한 번 보여준다.

**그다음 화면설계서를 여는 법을 말한다.** 링크가 아니라 파일이라서 이 줄이 꼭 필요하다.

```
화면설계서는 파일로 만들었어요. 두 번 누르면 브라우저에서 열립니다.

  {작업폴더}/outputs/04-design/화면설계서.html

인터넷에 안 올렸습니다. 이 컴퓨터 안에만 있어요.
```

**절대경로를 문서에 안 적는다.** 화면에 말할 때만 실제 경로를 쓴다.

그다음 답을 못 받은 질문이 있으면 **반드시 말한다.** 조용히 넘기지 않는다.

그리고 한 번 더 확인시킨다.

| | |
|---|---|
| 화면 | {N}종류입니다. 글 {N}편은 그중 한 틀을 같이 씁니다 |
| 03 산출물 | 전부 어느 화면엔가 자리를 가졌습니다 |
| 연락처 | 자리만 잡았습니다. **값은 문서에 안 적었습니다** |
| 레퍼런스 | 값만 가져왔습니다. 어디서 봤는지는 `근거.html` 에 있습니다 |
| 파일 | 인터넷에 안 올렸습니다 |

마지막으로 **링크와 설치 명령까지** 준다. 링크만 주고 끝내지 않는다.

```
다음은 05 TECH 입니다.
방금 그린 화면마다 검색과 AI 답변에 걸리는 규칙을 붙이는 단계입니다.
화면 {N}종류에 제목과 설명이 붙고, 주소를 어떻게 만들지도 거기서 정합니다.

  https://github.com/iny101400-young/iny-tech

받으시려면 이 폴더에서 아래를 그대로 치시면 됩니다.

  git clone https://github.com/iny101400-young/iny-tech.git
  cd iny-tech
  bash install.sh

깔고 나서 Claude Code 를 다시 열고 "기술 문서" 라고 치세요.
방금 만든 화면설계서.html 을 그대로 읽습니다.
```

**같은 링크를 `plan.md` 에도 적는다.** 화면은 스크롤되면 사라진다.

**그리고 맨 끝에 이 한 줄로 닫는다.**

```
쭉 보시고 고칠 데만 말씀해 주세요.
```

**이 줄 뒤에 아무것도 두지 않는다.** 여기가 끝이다.

---

## 04 가 정하는 것과 안 정하는 것

**03 이 여기서 한 번 걸렸다. 덜 해서가 아니라 더 해서 생긴 실수였다.**

> **앞 단계 산출물은 재료로 읽고, 뒤 단계 결정은 후보만 낸다.**

| | 04 가 정하나 | 왜 |
|---|---|---|
| 화면이 몇 종류고 각 화면에 뭐가 들어가나 | **정한다** | 이게 04 의 일이다 |
| 메뉴 구성 | **정한다** | 03 이 명시적으로 넘겼다 |
| 글 자리의 이름 | **정한다** | 03 이 넘겼다. 메뉴 글자와 붙어 있다 |
| 색 · 글꼴 · 간격 · 움직임 | **정한다** | 통과 조건에 「색상값 · 폰트 확정」이 있다 |
| 요소마다 다섯 층 | **정한다** | 06 이 이걸로 짓는다 |
| 어느 화면에 기계가 읽는 표시가 붙나 | **자리만 표시한다** | **값은 05** |
| 표시의 실제 값 · 계정 주소 코드 | **안 정한다** | 05 |
| robots · 사이트맵 · 제목과 설명 문구 | **안 정한다** | 05 |
| 주소를 한글로 둘지 · 갈래를 주소에 넣을지 | **안 정한다** | **03 이 후보 둘을 냈고 05 가 고른다** |
| 실제 색이 화면에서 어떻게 보이나 | **안 정한다** | 06 에서 지어봐야 안다 |
| 어느 글을 먼저 쓰나 | **안 정한다** | 07 |
| 차트 · 지표 화면 | **안 정한다** | 08 |

---

## 지켜야 할 것

| | |
|---|---|
| 순서 | 내부 상태를 첫 화면에 내지 않는다. **안내문이 먼저다** |
| 디자인 먼저 | **디자인을 정하고 화면을 그린다.** 거꾸로 하면 다시 그린다 |
| 장수 | **미리 안 박는다.** 조건 넷을 걸고 다 그린 뒤에 몇 장인지 말한다 |
| 묶기 | **화면 종류마다 한 장.** 묶은 것은 **몇 개를 덮는지 적는다** |
| 자리 | **03 이 준 것이 전부 어느 화면엔가 자리를 갖는다.** STEP 9 에서 기계로 잰다 |
| 빈 상태 | **글이 0편일 때의 화면을 같이 그린다** |
| 갈래와 메뉴 | **큰 갈래 수를 메뉴 수로 옮기지 않는다** |
| 예시 | **앞사람 산출물에서 안 가져온다. 지어내고 지어냈다고 적는다** |
| 레퍼런스 | **값만 가져온다.** 그림을 산출물에 안 넣는다 |
| 사진 | 화면설계서에 안 넣는다. **전부 CSS 로 그린다** |
| 미리보기 | **글로 안 적고 그린다.** 정한 색과 정한 글꼴로 |
| 다섯 층 | 파일 안은 영문 그대로. **05·06 이 그 이름으로 읽는다** |
| 「없음」 | 빈칸으로 두지 않는다. **정한 것과 안 정한 것을 가른다** |
| 화면 이름표 | **번호 + 한글 제목.** 영문 약자를 만들지 않는다. **번호를 안 민다** |
| 05 의 일 | **미리 정하지 않는다.** 자리까지만 |
| 개인정보 | **연락처 값을 문서에 안 적는다.** 「칸이 있다」까지만 |
| 발행 | **링크로 안 올린다.** 파일로만 낸다 |
| 절대경로 | 산출물에 안 적는다. **화면에 말할 때만 쓴다** |
| 자동 커밋 | 스킬이 알아서 커밋하지 않는다 |
| 검색 | **횟수를 약속하지 않는다.** 안 되면 안 하고 그대로 간다 |
| 무거운 것 | 첫 화면 뜨는 시간을 나쁘게 하는 것은 **그 값을 같이 말한다** |
| 손일 | **사람에게 안 넘긴다.** 내가 다 채워 놓고 사람은 짚는다 |
| 누가 하나 | **내가 할 일과 사람이 할 일을 갈라서 말한다** |
| 어디에 넣었나 | 파일에 넣고 **어디에 넣었는지 한 줄로 말한다** |
| 「사용자가 직접」 | **산출물에 이렇게 적지 않는다** |
| 「나중에 채움」 | 산출물에 안 쓴다. 못 채웠으면 **왜 못 채웠는지**를 쓴다 |
| 꼬리 | **STEP 10 이 마지막이다.** 확인받는 자리 뒤에 아무것도 두지 않는다 |
| 클릭 | **다섯 개.** 한 번에 하나씩 |
| 출력 | 화면 목록과 글 목록을 화면에 쏟지 않는다 |
| 시간 | **숫자로 약속하지 않는다** |
| 경로 | 이 파일에 적어두지 않는다. 설정에서 읽거나 사용자에게 묻는다 |
